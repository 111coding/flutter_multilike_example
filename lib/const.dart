import 'package:flutter/material.dart';

const kHorizontalPadding = 24.0;

SizedBox eWidth(double width) => SizedBox(width: width);

SizedBox eHeight(double height) => SizedBox(height: height);

double topPadding(BuildContext context) => MediaQuery.of(context).padding.top;
